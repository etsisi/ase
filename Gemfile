source "https://rubygems.org"

# Mismo conjunto de gemas y versiones que usa GitHub Pages en producción
gem "github-pages", group: :jekyll_plugins

# Necesario para `jekyll serve` con Ruby >= 3.0
gem "webrick", "~> 1.8"
